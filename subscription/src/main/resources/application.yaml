server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_invoice_db
    username: jbeanny
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
